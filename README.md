# Bingyan-Elm
使用Elm开发冰岩官网，以及附带自己的脚手架




```shell

npm install && elm install package // install the dependences

npm run start // start the server at localhost:7777

npm run build // build the production

```
